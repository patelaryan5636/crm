// clientsStore.js — initial seed re-exported from canonical store
// (TEAM_GUIDE Section 9 rule #2). The Clients page lifts this into local
// `useState` so MM can Add / Edit / update drive links.
export { clientList as clients } from "./managementManagerStore";
