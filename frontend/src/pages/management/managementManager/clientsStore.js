/**
 * clientsStore.js — DEPRECATED
 *
 * Clients are fetched live from GET /api/management/clients
 * in ManagementManagerClients.jsx. No mock data lives here.
 *
 * Safe to delete once all imports are removed.
 */
export {};
